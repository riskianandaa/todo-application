import SafeAreaWrapper from "@/components/shared/safe-area-wrapper";
import { Box, Text } from "@/utils/theme";
import React from "react";

const CategoryScreen = () => {
  return (
    <SafeAreaWrapper>
      <Box>
        <Text>Category in screen</Text>
      </Box>
    </SafeAreaWrapper>
  );
};

export default CategoryScreen;
